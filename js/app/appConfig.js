define(function () {

    return { 

        // urls
        URL_HOME            : siteSettings.ajaxUrl+'?action=portfolio',
        URL_PORTFOLIO       : siteSettings.ajaxUrl+'?action=portfolio',
        URL_BLOG            : siteSettings.ajaxUrl+'?action=blog',
        URL_CONTACT         : siteSettings.ajaxUrl+'?action=send_feedback',
        BASE_TITLE          : 'Maxim Denisov | ',

        hashDelimetr        : '#/'

    };
    
});